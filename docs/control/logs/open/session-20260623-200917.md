# F7 Session 20260623-200917

**Agent:** Agent A — Cert / Evidence / Git / PR  
**Branch:** `fix/f7-gate-stability` @ `cc6fbac`  
**Verdict:** **FAIL** (exit 2) — first heavy flush after `HeavyFlushUnblocked`  
**PR #7:** **HOLD**

## Manifest summary

| Field | Value |
|-------|-------|
| `passFail` | FAIL |
| `launchState` | `fail_obvious_post_spawn_death` |
| `launchPath` | continue |
| `targetMismatch` | false |
| `stableSeconds` | 0 |
| `evidenceCompleteness.score` | sufficient |
| Wall time | ~113s (poll 73s, 90s before fail) |

## B fix `cc6fbac` — partial validation

| Marker | Present |
|--------|---------|
| `StabilizationEnd` seq=8066 | yes |
| `HeavyFlushGraceBegin` seq=8067 | yes |
| `update_readiness_heavy stage=skipped reason=post_map_ready_stabilization` | yes (many cycles) |
| `HeavyFlushUnblocked` seq=8106 | yes |
| Death at seq=8115 `update_readiness begin` | yes — **after** unblocked |
| CrashContext `heavyFlushDeferred` | **false** at death |

**Progress vs `195817`:** Grace lifecycle ran; death moved from immediate post-`StabilizationEnd` to first post-unblock `FlushFull`. Root cause class unchanged: heavy status flush / posture scan kills process.

## C fix — still valid

Poll past ~61s (73s, 90s); harvest sufficient.

## Routing

**Agent B:** First `FlushFull` after `HeavyFlushUnblocked` must not kill — extend defer, fail-soft posture scan, or delay unblocked sync until orchestrator/map-ready stable.

## Evidence

`docs/evidence/live-cert/20260623-200917/checkpoint-01-f7-gate/`
