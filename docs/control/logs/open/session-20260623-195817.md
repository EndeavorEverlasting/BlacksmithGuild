# F7 Session 20260623-195817

**Agent:** Agent A — Cert / Evidence / Git / PR  
**Branch:** `fix/f7-gate-stability` @ `705d2be` (C fix) + `0e312e5` (B fix)  
**Verdict:** **FAIL** (exit 2) — runtime death post-stabilization  
**PR #7:** **HOLD**

## Manifest summary

| Field | Value |
|-------|-------|
| `passFail` | FAIL |
| `exitCode` | 2 |
| `launchState` | `fail_obvious_post_spawn_death` |
| `launchPath` | continue |
| `targetMismatch` | false |
| `stableSeconds` | 0 |
| `campaignReady` | true |
| `canPollFileInbox` | false |
| `evidenceCompleteness.score` | **sufficient** |
| Wall time | ~148s |

## Agent C fix validated

- Poll reached **110s** and **122s** (past `202052` false abort @ ~61s).
- Harvest: Status + CrashContext copied; `crashContextOptional=true`; no `harvest_failed`.

## Agent B partial fix — new death site

CrashContext @ seq=8063:

- `operation=update_readiness`, `stage=begin`
- **`stabilizationActive=false`** (window had ended)
- Phase1: `[TBG MAPREADY] post-map-ready stabilization window ended` immediately before death

**Interpretation:** `0e312e5` guarded heavy flush during 20s stabilization. After window ends, first full `update_readiness` / `FlushFull` (likely `FactionPowerPostureScanner`) kills process — same class as `192811` but **after** stabilization, not during.

Compare `202052`: survived through seq=2810 with stabilization skip markers; runner aborted early.

## Golden path

`firstMissingStep=MainMenu -> MapTransition` — map-ready orchestrator not completed before death.

## Routing

| Lane | Action |
|------|--------|
| **B** | Defer heavy flush/posture scan until map-ready orchestrator complete or extend guard past stabilization end |
| **C** | Idle — `705d2be` validated for 202052 pattern |
| **A** | Re-cert after B fix |

## Evidence

`docs/evidence/live-cert/20260623-195817/checkpoint-01-f7-gate/`
